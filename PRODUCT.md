# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- **P1 小白与日常用户**：把本地 AI 当数字同事，不希望面对复杂运维仪表盘，只想直观知晓“当前状态好不好、要不要我点一下”；遇到故障要听得懂通俗原因、看得到明确的下一步行动。
- **P2 轻度进阶运维**：需要管理通道连接、技能插件、定时任务与安全设置，偏好卡片和结构化列表，但排斥晦涩的底层代码术语。
- **P3 高级工程师与 SRE**：需要版本预检、升级回滚、Hermes Bridge 消息网关底层参数、进化守门证据、模型 Token 消耗核算与全链路行为审计。

## Product Purpose

Agent Butler 是面向本地 AI 运行时的“安全电脑管家”与操作控制台：替用户实时守望着宿主机上的自主智能体，解答“系统是否可用、是否存在未决风险、下一项定时任务何时执行”。

Hermes 是底层任务的唯一调度与执行引擎，Butler 承担控制面、安全阻断拦截与人机协同治理（HITL）。全方位守护智能体资产（定时任务、消息通知、技能插件、向量记忆、环境快照），保障系统在无人值守环境下的高可用与自愈能力。

## Positioning

本地优先（Local-First）的自主智能体安全管家：以通俗大白话与确定性为第一原则，专注宿主回环安全隔离、真实 Human-in-the-Loop（HITL）事中阻断审批与无人值守自愈运维。不依赖云端托管，数据不离机。

## Operating Context

- **Web 响应式全平台**：以桌面端（>1024px）与平板为主生产力工作台，同时完整支持移动端响应式巡检、待办审批与应急熔断（通过顶栏汉堡菜单与底部 TabBar 实现全量 23 路由可达性）。
- **日常运维闭环**：
  1. 打开首页即获知系统健康结论与待决事项；
  2. 智能体越权或高危操作在执行前被阻断并进入 `/approvals` 队列等待人工决策；
  3. 异常与通道阻塞由排障向导自动静默体检并给出单键修复建议；
  4. 遇到失控行为可通过顶栏常驻的全局紧急熔断开关（Kill Switch）一键冻结并自动落盘系统快照。

## Capabilities and Constraints

- **核心功能**：
  - **首页控制台**：系统健康态势判定、待办审批引导、核心守护矩阵、真实网络 RTT 遥测。
  - **定时任务**：Hermes 调度任务管理、时区只读展示与执行历史溯源。
  - **消息网关**：Hermes Bridge 待处理消息队列（失败、未知、待确认），通道白名单配置与优雅热重启。
  - **智能体与记忆**：技能插件市场、Mem0/SQLite-VSS 本地记忆库与 SHA-256 内容绝对幂等去重。
  - **安全与审计**：HITL 拦截决策、模型 Token 用量成本统计、会话级操作日志审计。
  - **维护工具箱**：一键自动化排障向导、版本平滑升级与环境快照回滚、自进化守门。
- **技术与安全约束**：
  - Hermes Bridge 强制监听宿主 loopback 回环（127.0.0.1），严禁直接暴露到公网；
  - 未配置访问口令（`BUTLER_ACCESS_TOKEN`）时禁止将 Web 控制台发布到外部网络；
  - 严禁删除或轮换 `.env` 中的 `BUTLER_SECRET_MASTER_KEY`（历史凭据将无法解密）；
  - 镜像与源码构建禁止在 Windows NTFS 挂载路径（`/mnt/c`）上执行，避免文件锁竞态。

## Brand Commitments

- **品牌人格**：靠谱、亲切、克制、像能放心的本机管家。界面先给大白话结论，再给清晰的一个动作；状态明确，不吓人也不炫技。不夸大、不伪造、不贩卖无意义的科技感。
- **反面参考 (Anti-References)**：
  - 黑客运维面板：纯黑背景、霓虹渐变、高频闪烁状态灯。
  - 晦涩专业控制台：满屏硬编码底层术语（如 SQLite-WAL、Runbook、confidence、PASSIVE_OBSERVER）。
  - 消费级营销页：空洞宣传口号、插画占位、夸张 slogan。
  - 表象仿真与伪造安全感：使用演示数据、假图表波形或无后端联动的 Mock 按钮冒充真实能力。

## Evidence on Hand

严格遵守四大实证红线，严禁在未来工作流中凭空捏造：
1. **遥测真实**：网络延迟与连通状态必须来自真实探针（如 `/api/health` RTT），离线与断开时真实告警，绝不使用随机数兜底；
2. **审批真实**：HITL 审批与高危阻断必须直连后端审批队列，绝无前端本地 Mock 状态或伪造的硬件私钥签名；
3. **资产真实**：知识库清理仅限 SHA-256 内容绝对一致的重复副本，严禁未经二次确认模糊误删；
4. **审计真实**：模型 Token 消耗计费与会话行为链条提供逐条真实记录，不可编造数据。

## Product Principles

1. **先给结论，再给动作**：首屏第一眼直接回答“当前是否正常、要不要我处理”，结论必须来自真实数据，高危告警附带直达动作。
2. **真实边界与绝对确定性**：未接入、未运行、未完成、离线均如实说明；未实现能力显式禁用，绝不使用 Mock 假数据冒充功能。
3. **破坏性操作强制防御**：会修改数据、重启服务、升级回滚或清空资产的操作，必须触发二次确认声明，并在熔断时自动落盘快照。
4. **术语通俗化与渐进披露**：主流程使用自然大白话，底层技术明细（错误堆栈、调度参数、网络规格）收进可折叠的“高级详情”。
5. **语义与视觉严谨统一**：正常、待处理、异常、离线、未知在全站保持唯一的颜色、状态徽标与文本定义。

## Accessibility & Inclusion

- 目标 WCAG AA：正文对比度 ≥ 4.5:1，大文本 ≥ 3:1；
- 全键盘可操作，焦点指示清晰；不用单一颜色区分状态；
- 触控与点击目标默认 ≥ 44px（包括桌面粗指针与移动设备）；
- 中英文混排保持优雅可读，置顶系统级 Inter 字体栈。
