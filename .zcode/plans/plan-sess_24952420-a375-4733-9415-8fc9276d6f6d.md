面板六项 UX 优化，按 4 批实施（A→D），每批独立提交并过测试，最后统一部署验证并推送 GitHub。

**批 A（①+②，消息面板，纯前端）**
- `ui/src/pages/gateway/PromptOptimizationPanel.tsx`：
  - 对照历史列表收敛——每条默认折叠为单行摘要（状态+时间+内容首行），点击展开显示原文/整理后全文对照；新增按年月日 Select 快筛 + antd Pagination（每页 8 条，单页隐藏）；默认只看最近一页。
  - 「高级：规则文件改进记录」改名「提示词版本管理（实验功能）」并加人话说明；gateStatus 映射重写（ok=与官方一致绿 / hash-mismatch=文件有手动修改黄 / protected-clause-mismatch=关键段有改动黄 / 仅 missing=红）；候选状态去红色化（rejected-*=灰「未采用」、approval-pending=蓝、kept-baseline/promoted=绿）。
  - 轮询 10s→60s；请求 limit 300→100。
- helpers 纯函数测试（日期分组、状态文案映射）。

**批 B（③，降焦虑 + 转发给智能体）**
- `ui/src/pages/dashboard/conclusions.ts`：fingerprint status=known → tone 改 warn、文案「同类问题持续跟踪中（累计 N 次），功能正常可忽略」；open 保持红。
- `ui/src/pages/dashboard/IssuesSection.tsx`：每条新增「复制求助提示词」（拼结构化提示词+剪贴板）与「转发给智能体」按钮（仅 Hermes 实例显示；OpenClaw 只显示复制）。
- 新 web 端点 `POST /api/agent-message`：复用 packages/adapters/hermes 现成 api_server host/port/key 配置读取，转发提示词到 Hermes api_server 聊天接口（实现期核对 api_server.py 实际路由/鉴权，30s 超时），返回响应文本；web 测试覆盖。

**批 C（④，连接状态一行化，纯前端）**
- `ui/src/pages/dashboard/ConnectionSection.tsx`：六项检查改单行横排（label + StatusBadge，Tooltip 显示详情/耗时）；Descriptions 压缩为单行关键信息，次要项移入悬停。

**批 D（⑤+⑥，就绪度信息图）**
- watch 新端点 `GET /api/host/metrics`：machine（CPU% 两次采样差/内存/df 磁盘/uptime/负载/GPU 可选探测）+ agents[]（每实例 CPU%/RSS，结构化现有 ResourceSample）+ samples[] 环形缓冲；照现有 http.ts 路由模式。
- web 透传 `/api/host/metrics`（503 降级）；`probeServiceHealth` 增加耗时。
- `ui/src/pages/dashboard/ReadinessSection.tsx` 追加「Agent 主机状态」（CPU/内存/磁盘进度条 + GPU + 多实例 antd Tabs）与「管家运行指标」（巡检耗时 14 天 Sparkline + 连接延迟 + 记忆探针耗时 + Bridge 轮询新鲜度 + 各服务延迟）两卡；useDashboardData 增加 30s 轮询。
- fingerprint 高级详情表 known 状态去红色化。

**收尾**：全量 tsc + vitest；`deploy.sh` 部署本机逐项线上核验；CHANGELOG + 版本号 bump；推送 GitHub。